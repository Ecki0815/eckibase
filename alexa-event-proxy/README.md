# Alexa Event Proxy v0.2

Features:
- Flask + Flask-Sock
- Bearer auth
- Basic Auth via nginx
- WebSocket broadcast
- Heartbeat endpoint
- Structured logging
- Queue-based dispatcher (skeleton)
- Docker Compose
